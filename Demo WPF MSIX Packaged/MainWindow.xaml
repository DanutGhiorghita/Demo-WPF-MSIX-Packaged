<Window x:Class="Demo_WPF_MSIX_Packaged.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Demo_WPF_MSIX_Packaged"
        mc:Ignorable="d"
        Title="File Browser" Height="500" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Folder selection row -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="FolderPathTextBox"
                     Grid.Column="0"
                     VerticalContentAlignment="Center"
                     Margin="0,0,6,0"
                     IsReadOnly="True"/>
            <Button x:Name="BrowseButton"
                    Grid.Column="1"
                    Content="Browse..."
                    Width="90"
                    Click="BrowseButton_Click"/>
        </Grid>

        <!-- Toolbar buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button x:Name="ListFilesButton"
                    Content="List Files"
                    Width="90"
                    Margin="0,0,6,0"
                    IsEnabled="False"
                    Click="ListFilesButton_Click"/>
            <Button x:Name="EnvPathsButton"
                    Content="Env Paths"
                    Width="90"
                    Click="EnvPathsButton_Click"/>
        </StackPanel>

        <!-- File list -->
        <ListView x:Name="FilesListView"
                  Grid.Row="2"
                  VirtualizingPanel.IsVirtualizing="True"
                  GridViewColumnHeader.Click="FilesListView_ColumnHeaderClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="280" DisplayMemberBinding="{Binding Name}">
                        <GridViewColumnHeader Content="Name" Tag="Name"/>
                    </GridViewColumn>
                    <GridViewColumn Width="120" DisplayMemberBinding="{Binding Size}">
                        <GridViewColumnHeader Content="Size (bytes)" Tag="Size"/>
                    </GridViewColumn>
                    <GridViewColumn Width="180" DisplayMemberBinding="{Binding LastModified}">
                        <GridViewColumnHeader Content="Last Modified" Tag="LastModified"/>
                    </GridViewColumn>
                    <GridViewColumn Width="100" DisplayMemberBinding="{Binding Type}">
                        <GridViewColumnHeader Content="Type" Tag="Type"/>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Status bar -->
        <TextBlock x:Name="StatusTextBlock"
                   Grid.Row="3"
                   Margin="0,6,0,0"
                   Foreground="Gray"/>
    </Grid>
</Window>
