<Window x:Class="Demo_WPF_MSIX_Packaged.EnvironmentPathsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Environment Path Variables" Height="520" Width="780"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search box -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Filter:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox x:Name="FilterTextBox"
                     Grid.Column="1"
                     VerticalContentAlignment="Center"
                     TextChanged="FilterTextBox_TextChanged"/>
        </Grid>

        <!-- Environment variables list -->
        <ListView x:Name="EnvListView"
                  Grid.Row="1"
                  VirtualizingPanel.IsVirtualizing="True"
                  GridViewColumnHeader.Click="EnvListView_ColumnHeaderClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="220" DisplayMemberBinding="{Binding Name}">
                        <GridViewColumnHeader Content="Variable" Tag="Name"/>
                    </GridViewColumn>
                    <GridViewColumn Width="460" DisplayMemberBinding="{Binding Value}">
                        <GridViewColumnHeader Content="Value" Tag="Value"/>
                    </GridViewColumn>
                    <GridViewColumn Width="80" DisplayMemberBinding="{Binding Scope}">
                        <GridViewColumnHeader Content="Scope" Tag="Scope"/>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Status / close -->
        <Grid Grid.Row="2" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusTextBlock" Grid.Column="0" VerticalAlignment="Center" Foreground="Gray"/>
            <Button Grid.Column="1" Content="Close" Width="80" Click="CloseButton_Click"/>
        </Grid>
    </Grid>
</Window>
